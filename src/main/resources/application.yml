# redis配置server:  port: 80spring:  application:    name: "aca"  redis:    host: 10.10.66.178    port: 6379    database: 0    password:    timeout: 1000    jedis:      pool:        max-active: 20        max-wait: -1        max-idle: 10        min-idle: 0#数据库配置  datasource:    platform: postgres    url: jdbc:postgresql://10.10.66.178:5432/aca-webapp    username: shterm    password: shterm    driver-class-name: org.postgresql.Driver  jpa:    show-sql: true    hibernate:      ddl-auto: none # 可设置是否每次都清库    database: postgresql  liquibase:    change-log: classpath:/db/changelog/aca-changelog.xml    # 默认路径为/template目录，此处设置为static路径  thymeleaf:    prefix: classpath:/static/  